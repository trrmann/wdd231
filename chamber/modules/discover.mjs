export async function DisplayDiscoverInformation(discoverMainContainerClass) {
    const discoverMainContainer = document.querySelector(discoverMainContainerClass);

    discoverMainContainer.textContent = 'discover';

}